export const javascript = `exports.pathfinder = pathfinder;
function pathfinder(n, m, matrix, x1, y1, x2, y2) {
  return;
}`;

export const ruby = `class Pathfinder
  def initialize(n, m, matrix, x1, y1, x2, y2)
  end
  def solve
  end
end`;
